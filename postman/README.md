# Helper Postman collection

Import `order-sync-helper.postman_collection.json` and `order-sync.local.postman_environment.json`. The collection covers dashboard, mock-SAP, and Adminer development tooling. The order-sync API collection remains in the order-sync repository's `postman` directory.
